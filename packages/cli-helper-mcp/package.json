{
  "name": "cli-helper-mcp",
  "version": "1.0.0",
  "description": "CLI Helper MCP Server for Slay the Spire 2 Mod Development",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && esbuild src/picker/picker.ts --bundle --outfile=dist/picker.js --target=es2020 --format=iife",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "esbuild": "^0.27.4",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
